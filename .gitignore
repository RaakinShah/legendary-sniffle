__pycache__/
*.pyc
*.egg-info/
build/
dist/
.venv/
venv/

# Secrets — never commit these
.env
mcp_servers.json

# Local assistant data (lives in ~/.assistant by default, but just in case)
.assistant/

# macOS Finder junk
.DS_Store

# compiled Apple FM bridge binary (build with bridge/build.sh)
bridge/aide-fm
